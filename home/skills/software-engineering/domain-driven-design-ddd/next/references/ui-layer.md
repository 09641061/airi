# UI Layer

Keep UI code in `interfaces/`.

1. Presentational components render UI only.
2. Forms transform view data into commands.
3. Resources represent transport shapes, not domain shapes.
4. Explicit transforms are allowed; mapper classes are not.
